<h1><%= @task.title.capitalize %></h1>
<div>
  <%= @task.details %>
</div>

<% if @task.completed %>
  <p>Completed task!</p>
<% else %>
  <p>This task is not completed yet.</p>
<% end %>

<%= link_to 'Back to list', tasks_path %>
